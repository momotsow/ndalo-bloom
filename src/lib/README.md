# Lib

Shared cross-cutting utilities (config access helpers, logging, money helpers, generic
pure utilities). Must not contain business rules (those live in Domain) and must not import
UI, Prisma, provider SDKs, or Better Auth.
